class PhoneNumber:
    def __init__(self, number):
        self.raw_number = number
        self.valid_numbers

    def get_numbers(self):
        for digit in self.raw_number:

    def check_length(number):
        if len(number) < 10
